id: java-empty-catch
language: java
severity: warning
message: "Empty catch block — the exception is swallowed silently (charter: failure must be loud)"
rule:
  pattern:
    context: "try {} catch ($T $E) {}"
    selector: catch_clause
